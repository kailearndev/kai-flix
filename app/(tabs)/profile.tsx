import { Text, View } from "react-native";

const Profile = () => {
  return (
    <View className="flex-1 items-center justify-center bg-black">
      <Text className="text-white">
        Edit app/(tabs)/profile.tsx to edit this screen.
      </Text>
    </View>
  );
};

export default Profile;
